[tool.poetry]
name = "dataroad-analytics"
version = "0.1.0"
description = "DataRoad Analytics - Lambdas"
authors = ["Vinicius"]
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"

[tool.poetry.group.extractor.dependencies]
boto3 = "^1.34"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
